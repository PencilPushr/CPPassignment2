#include "Manager.h"

Manager::Manager(const string& name, int regNo) : Person(name, regNo)
{
	//this->name = name;
	//this->regNo = regNo;
}

int Manager::getRegNo() const
{
	return 0;
}
